# files and directories ignored by the wp dist-archive command

# plugin things
.distignore
docs
phpunit.xml
tests

# git things
.git
.gitignore

# composer things
composer.json
composer.lock
vendor/composer
vendor/autoload.php

# wp_mock
vendor/10up
vendor/antecendent
vendor/hamcrest
vendor/mockery

# shave of some weight off Twig
vendor/twig/doc
vendor/twig/ext
vendor/twig/test
